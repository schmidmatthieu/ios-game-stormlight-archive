{
  "buildCommand": "cd cosmere-web && npm install && npm run build",
  "outputDirectory": "cosmere-web/dist",
  "framework": null,
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" }
  ]
}
